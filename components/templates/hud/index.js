const HUDTemplate = ({ name, coins }) => (
    <aside>
      <style jsx>{`
      `}</style>
      <h5>{ name }</h5>
      <hr />
      <p>Coins: { coins }</p>
    </aside>
)

export default HUDTemplate